services:
  redis:
    image: redis:7
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      # Unsafe unless access is restricted by a separate network control.
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
